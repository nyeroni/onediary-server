
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/onediary?useSSL=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: jsyr
    password: 00050826
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        #show_sql: true
        format_sql: true
        default_batch_fetch_size: 100
        dialect: org.hibernate.dialect.MySQLDialect
        dialect.storage_engine: innodb
#    open-in-view: false

  profiles:
    include: oauth

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace

#jwt:
#  header: Authorization
#  #HS 알고리즘을 사용할 것이기 때문에 512bit, 즉 64byte 이상의 secret key를 사용해야 한다.
#  #echo 'silvernine-tech-spring-boot-jwt-tutorial-secret-silvernine-tech-spring-boot-jwt-tutorial-secret'|base64
#  secret: c2lsdmVybmluZS10ZWNoLXNwcmluZy1ib290LWp3dC10dXRvcmlhbC1zZWNyZXQtc2lsdmVybmluZS10ZWNoLXNwcmluZy1ib290LWp3dC10dXRvcmlhbC1zZWNyZXQK
#  access:
#    expiration: 3600000 # 1시간(60분) (1000L(ms -> s) * 60L(s -> m) * 60L(m -> h))
#    header: Authorization
#  refresh:
#    expiration: 1209600000 #  (1000L(ms -> s) * 60L(s -> m) * 60L(m -> h) * 24L(h -> 하루) * 14(2주))
#    header: Authorization-refresh
#
#oauth2:
#  authorizedRedirectUris: "https://localhost:8080"

springdoc:
  packages-to-scan: onediary.onediary
  default-consumes-media-type: application/json;charset=UTF-8
  default-produces-media-type: application/json;charset=UTF-8
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /api-docs/json
    groups:
      enabled: true
  cache:
    disabled: true
#server:
#  ssl:
#    key-store: keystore.p12
#    key-store-type: PKCS12
#    key-store-password: skdmsdlWkd93!@
#  port: 8443

jwt.secret: 'b25lZGlhcnktdGVjaC1zcHJpbmctYm9vdC1qd3Qtc2VjcmV0LW9uZWRpYXJ5LXRlY2gtc3ByaW5nLWJvb3Qtand0LXNlY3JldA0K'
# cors 설정
cors:
  allowed-origins: 'http://localhost:8080'
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: '*'
  max-age: 3600