spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

app:
  auth:
    tokenSecret: secretkeysecretkeysecretkeysecretkeysecretkeysecretkey
    tokenExpiry: 2592000000 # 30일

swagger-info:
  title: OneDiary REST API
  description: OneDiary
  version: 1.0.0

cloud:
  aws:
    stack:
      auto: false
    credentials:
      instanceProfile: true
      access-key: test
      secret-key: test
    region:
      static: us-east-2
    s3:
      bucket: testBucket